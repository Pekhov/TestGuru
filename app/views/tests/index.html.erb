<div class="row justify-content-center mt-3">
  <div class="col col-md-10 ">
    <h1 align=center><%= t('.header') %></h1>

    <table class="table table-bordered">
      <tr>
        <th><%= t('.title') %></th>
        <th><%= t('.level') %></th>
        <th><%= t('.questions_number') %></th>
        <th><%= t('.action') %></th>
      </tr>
      <%= render @tests %>
    </table>
  </div>
</div>
